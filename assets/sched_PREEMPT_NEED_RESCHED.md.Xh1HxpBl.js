import{_ as e,c as n,o as a,al as i}from"./chunks/framework.CdzZEkix.js";const h=JSON.parse('{"title":"PREEMPT_NEED_RESCHED","description":"","frontmatter":{"head":[["meta",{"property":"og:title","content":"PREEMPT_NEED_RESCHED | Blog"}]]},"headers":[],"relativePath":"sched/PREEMPT_NEED_RESCHED.md","filePath":"sched/PREEMPT_NEED_RESCHED.md","lastUpdated":1763912647000}'),t={name:"sched/PREEMPT_NEED_RESCHED.md"};function r(l,s,p,d,o,E){return a(),n("div",null,[...s[0]||(s[0]=[i(`<h1 id="preempt-need-resched" tabindex="-1">PREEMPT_NEED_RESCHED <a class="header-anchor" href="#preempt-need-resched" aria-label="Permalink to “PREEMPT_NEED_RESCHED”">​</a></h1><p>AI 的讲解，存在一些错误。</p><h2 id="gemini-3-pro" tabindex="-1">Gemini 3 Pro <a class="header-anchor" href="#gemini-3-pro" aria-label="Permalink to “Gemini 3 Pro”">​</a></h2><p>在 Linux 内核中，<code>PREEMPT_NEED_RESCHED</code> 是一个<strong>用于优化抢占检查的高级宏</strong>，它位于 <code>preempt_count</code>（抢占计数器）的高位中。</p><p>它的核心作用是：<strong>将“是否允许抢占”和“是否需要抢占”这两个检查合并为一个操作，从而极大地优化 <code>preempt_enable()</code> 这一热点路径的性能。</strong></p><p>以下是详细的深度解析：</p><h3 id="_1-背景-为什么需要它" tabindex="-1">1. 背景：为什么需要它？ <a class="header-anchor" href="#_1-背景-为什么需要它" aria-label="Permalink to “1. 背景：为什么需要它？”">​</a></h3><p>在 Linux 内核中，<code>preempt_enable()</code> 是一个调用频率极高的函数（成千上万次/秒），因为它成对出现在每一个 <code>spin_lock</code> / <code>spin_unlock</code> 中。</p><p><strong>传统的（慢速）逻辑</strong>是这样的： 当调用 <code>preempt_enable()</code> 时，内核需要做两件事：</p><ol><li>将抢占计数器减 1 (<code>dec preempt_count</code>)。</li><li>检查计数器是否变为 0（意味着现在允许抢占了）。</li><li><strong>如果为 0</strong>，则再去检查当前线程的 <code>thread_info-&gt;flags</code> 里是否有 <code>TIF_NEED_RESCHED</code> 标志（意味着调度器想切走 CPU）。</li><li>如果两者都满足，调用 <code>schedule()</code>。</li></ol><p><strong>问题</strong>：第 2 步和第 3 步涉及访问两个不同的内存位置（一个在 <code>preempt_count</code>，一个在 <code>thread_info</code>），这在 CPU 流水线上效率不够极致。</p><h3 id="_2-preempt-need-resched-的机制-折叠检查-folding" tabindex="-1">2. <code>PREEMPT_NEED_RESCHED</code> 的机制：折叠检查 (Folding) <a class="header-anchor" href="#_2-preempt-need-resched-的机制-折叠检查-folding" aria-label="Permalink to “2. PREEMPT_NEED_RESCHED 的机制：折叠检查 (Folding)”">​</a></h3><p>为了优化，内核引入了 <strong>&quot;Folded need_resched&quot;</strong> 技术。</p><p><code>PREEMPT_NEED_RESCHED</code> 通常被定义为 <code>preempt_count</code> 变量的最高位（例如第 31 位，<code>0x80000000</code>）。</p><p>内核使用了一种<strong>反向逻辑（Inverted Logic）</strong>来利用这个位：</p><ul><li><strong>平常状态（无需调度）</strong>： 该位被<strong>置为 1</strong>。 因此，即使抢占深度（Preempt Depth）为 0，<code>preempt_count</code> 的整体值也是非零的（因为它等于 <code>0x80000000</code>）。</li><li><strong>调度请求状态（需要调度）</strong>： 当调度器设置 <code>TIF_NEED_RESCHED</code> 时，它会同时<strong>清除</strong> <code>preempt_count</code> 中的 <code>PREEMPT_NEED_RESCHED</code> 位（置为 0）。</li></ul><h3 id="_3-优化后的-preempt-enable-流程" tabindex="-1">3. 优化后的 <code>preempt_enable()</code> 流程 <a class="header-anchor" href="#_3-优化后的-preempt-enable-流程" aria-label="Permalink to “3. 优化后的 preempt_enable() 流程”">​</a></h3><p>有了这个机制，<code>preempt_enable()</code> 的汇编逻辑变得极其简单：</p><div class="language-c line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 伪代码逻辑</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> preempt_enable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 1. 减少抢占计数（同时也会影响是否为 0 的判断）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dec_and_test_zero</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">preempt_count)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 只有当：</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // A. 抢占深度减完后是 0 (允许抢占)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        //    AND</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // B. PREEMPT_NEED_RESCHED 位是 0 (需要抢占)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 整个变量才会变成 0。</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        __preempt_schedule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p><strong>效果</strong>：内核只需要一条 <code>DEC</code> 指令和一条 <code>JZ</code> (Jump if Zero) 指令，就能同时判断“锁是否释放干净”以及“是否有任务在等待 CPU”。只有当两者同时满足时，才会进入调度路径。</p><h3 id="_4-总结" tabindex="-1">4. 总结 <a class="header-anchor" href="#_4-总结" aria-label="Permalink to “4. 总结”">​</a></h3><p><code>PREEMPT_NEED_RESCHED</code> 是一个<strong>优化技巧</strong>。</p><ul><li><strong>定义</strong>：<code>preempt_count</code> 变量中的一个特殊位（通常是最高位）。</li><li><strong>作用</strong>：作为 <code>TIF_NEED_RESCHED</code> 标志在抢占计数器里的“影子”。</li><li><strong>目的</strong>：让 <code>preempt_enable()</code> 能够通过单次原子减法和零值检查，快速决定是否触发抢占，消除了额外的内存访问和分支判断。</li></ul><p><em>注：这一机制主要用于支持该优化的架构（如 x86），在某些架构或配置下，可能依然沿用传统的分离检查方式。</em></p><h2 id="知乎直答" tabindex="-1">知乎直答 <a class="header-anchor" href="#知乎直答" aria-label="Permalink to “知乎直答”">​</a></h2><p><strong>PREEMPT_NEED_RESCHED 宏是 Linux 内核抢占机制的核心标志位</strong>，它通过设置 <code>preempt_count</code> 变量的最高位（0x80000000），向内核发出「需要立即重新调度进程」的信号。这个标志与进程描述符中的 <code>TIF_NEED_RESCHED</code> 同步，形成了内核抢占调度的双保险机制。</p><h3 id="核心作用-触发内核抢占的「信号灯」" tabindex="-1"><strong>核心作用：触发内核抢占的「信号灯」</strong> <a class="header-anchor" href="#核心作用-触发内核抢占的「信号灯」" aria-label="Permalink to “核心作用：触发内核抢占的「信号灯」”">​</a></h3><p>当 PREEMPT_NEED_RESCHED 被置位时，它会在以下场景强制内核检查调度需求：</p><ol><li><strong>内核抢占安全点</strong>：如 <code>preempt_enable()</code> 调用时，内核会检查该标志并触发 <code>schedule()</code>。</li><li><strong>中断返回路径</strong>：硬件中断处理完成后，若返回内核态且该标志被设置，会立即抢占当前进程。</li></ol><p>与仅作用于用户态返回的 <code>TIF_NEED_RESCHED</code> 不同，PREEMPT_NEED_RESCHED 是<strong>内核态抢占的直接触发器</strong>。例如，当高优先级进程被唤醒时，内核会通过 <code>set_preempt_need_resched()</code> 设置该标志，确保当前内核路径执行到安全点后立即调度。</p><h3 id="实现机制-与-tif-need-resched-的协同工作" tabindex="-1"><strong>实现机制：与 TIF_NEED_RESCHED 的协同工作</strong> <a class="header-anchor" href="#实现机制-与-tif-need-resched-的协同工作" aria-label="Permalink to “实现机制：与 TIF_NEED_RESCHED 的协同工作”">​</a></h3><p>PREEMPT_NEED_RESCHED 与 <code>TIF_NEED_RESCHED</code> 通常成对设置，形成互补：</p><div class="language-c line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 设置调度需求时同步双标志</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resched_curr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rq) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    set_tsk_need_resched</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(curr);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 设置进程的TIF_NEED_RESCHED</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    set_preempt_need_resched</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 设置PREEMPT_NEED_RESCHED</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 调度完成后同步清除</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__schedule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    clear_tsk_need_resched</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prev);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 清除TIF_NEED_RESCHED</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    clear_preempt_need_resched</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 清除PREEMPT_NEED_RESCHED</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>这种设计避免了频繁读取 <code>task_struct</code> 结构体，通过 <code>preempt_count</code> 寄存器级别的检查提升调度效率。</p><h3 id="触发场景-何时会设置该标志" tabindex="-1"><strong>触发场景：何时会设置该标志？</strong> <a class="header-anchor" href="#触发场景-何时会设置该标志" aria-label="Permalink to “触发场景：何时会设置该标志？”">​</a></h3><ol><li><strong>时间片耗尽</strong>：CFS 调度器中，当进程运行时间超过 <code>ideal_runtime</code> 时，<code>check_preempt_tick()</code> 会调用 <code>resched_curr()</code> 设置标志。</li><li><strong>高优先级进程唤醒</strong>：如通过 <code>wake_up_process()</code> 唤醒实时进程时，会触发标志设置。</li><li><strong>周期性调度器 tick</strong>：<code>scheduler_tick()</code> 会检查就绪队列，必要时通过 <code>task_tick_fair()</code> → <code>entity_tick()</code> 设置标志。</li></ol><h3 id="与用户态抢占的区别" tabindex="-1"><strong>与用户态抢占的区别</strong> <a class="header-anchor" href="#与用户态抢占的区别" aria-label="Permalink to “与用户态抢占的区别”">​</a></h3><table tabindex="0"><thead><tr><th><strong>抢占类型</strong></th><th><strong>标志位</strong></th><th><strong>触发时机</strong></th><th><strong>典型场景</strong></th></tr></thead><tbody><tr><td>用户态抢占</td><td>TIF_NEED_RESCHED</td><td>系统调用/中断返回用户空间前</td><td><code>exit_to_user_mode_loop</code></td></tr><tr><td>内核态抢占</td><td>PREEMPT_NEED_RESCHED</td><td>抢占计数归零时（如 preempt_enable）</td><td><code>preempt_schedule_irq</code></td></tr></tbody></table><p>PREEMPT_NEED_RESCHED 的存在，使得 Linux 内核在支持<strong>可抢占内核</strong>（CONFIG_PREEMPT=y）时，能在中断处理、锁释放等场景实现低延迟调度。</p><h3 id="安全与性能的平衡" tabindex="-1"><strong>安全与性能的平衡</strong> <a class="header-anchor" href="#安全与性能的平衡" aria-label="Permalink to “安全与性能的平衡”">​</a></h3><p>PREEMPT_NEED_RESCHED 被包含在 <code>preempt_count</code> 变量中，后者的低 27 位记录抢占禁用层级。这种设计确保了：</p><ul><li><strong>安全抢占</strong>：仅当抢占计数为 0 且该标志置位时才会触发调度。</li><li><strong>性能优化</strong>：通过位运算快速检查标志，避免复杂的条件判断。</li></ul><p>例如，内核通过 <code>need_resched()</code> 宏统一检查调度需求，该宏会同时验证 PREEMPT_NEED_RESCHED 和 TIF_NEED_RESCHED。</p><p><strong>总结</strong>：PREEMPT_NEED_RESCHED 是 Linux 内核实现低延迟调度的关键组件，它像一个快速响应的「调度闹钟」，确保高优先级任务能在安全时机抢占 CPU，同时通过与 <code>TIF_NEED_RESCHED</code> 的协同，兼顾了用户态和内核态的调度需求。这个设计体现了 Linux 调度器在性能与实时性之间的精妙平衡。</p>`,44)])])}const _=e(t,[["render",r]]);export{h as __pageData,_ as default};
